---
alwaysApply: true
---

# Coffee Flow Project Rules

## Общие правила проекта Coffee Flow

Этот файл содержит ссылки на все правила проекта. Каждое правило применяется автоматически при работе с соответствующими файлами.

### 📚 Список правил

#### 🎨 [Цветовая система](./colors.mdc)

- Правила использования цветов из палитры Colors.ts
- Запрет на хардкодные цвета
- Использование хука useColors для темизации

#### 🏗️ [Архитектура](./architecture.mdc)

- Модульная структура проекта
- Правила организации файлов и папок
- Стандарты для auth, coffee-shops и shared модулей
- **НОВОЕ**: Структура экранов в папке `app/` с группировкой по модулям

#### ⚛️ [React компоненты](./components.mdc)

- **ОБЯЗАТЕЛЬНОЕ использование React Native Elements** для базовых UI компонентов
- Структура и типизация компонентов
- Правила стилизации и логики
- Оптимизация производительности
- **НОВОЕ**: Правила для экранных компонентов в папке `app/`

#### 🪝 [React хуки](./hooks.mdc)

- Структура и типизация хуков
- Управление состоянием и побочными эффектами
- Обработка ошибок и оптимизация

#### 🔷 [TypeScript](./typescript.mdc)

- Строгая типизация всех сущностей
- Generic типы и utility types
- Union types и discriminated unions

#### 📚 [Использование библиотек](./libraries.mdc)

- **ВСЕГДА сначала искать готовые решения** перед написанием кастомного кода
- Использование проверенных библиотек для сложных UI компонентов
- Интеграция с архитектурой и дизайн-системой проекта

### 🚀 Быстрый старт

1. **Новый компонент**: Следуйте правилам из `components.mdc` (ОБЯЗАТЕЛЬНО используйте React Native Elements)
2. **Новый хук**: Используйте правила из `hooks.mdc`
3. **Новые типы**: Следуйте правилам из `typescript.mdc`
4. **Новые цвета**: Используйте только палитру из `colors.mdc`
5. **Новая структура**: Следуйте архитектуре из `architecture.mdc`
6. **Готовые решения**: Следуйте правилам из `libraries.mdc` (ВСЕГДА ищите готовые библиотеки)
7. **Новые экраны**: Размещайте в папке `app/` с группировкой по модулям

### 📁 Структура проекта

```
app/                          # Экранные компоненты (Expo Router)
├── _layout.tsx              # Корневой layout с провайдерами
├── index.tsx                # Главная страница
├── onboarding.tsx           # Онбординг
├── profile.tsx              # Профиль пользователя
├── favorites.tsx            # Избранные кофейни
├── orders.tsx               # История заказов
├── checkout.tsx             # Оформление заказа
├── cart.tsx                 # Корзина
├── menu.tsx                 # Меню кофейни
├── auth/                    # Модуль аутентификации
│   └── login.tsx           # Экран входа
└── coffee-shops/            # Модуль кофеен
    ├── index.tsx            # Список кофеен
    └── [id].tsx             # Детальная страница кофейни

src/
├── auth/                    # Модуль аутентификации
│   ├── components/         # UI компоненты модуля (React Native Elements)
│   ├── hooks/             # Хуки модуля
│   ├── validation/        # Валидация модуля
│   └── index.ts           # Публичный API модуля
├── coffee-shops/           # Модуль кофеен
│   ├── components/        # UI компоненты модуля (React Native Elements)
│   ├── hooks/             # Хуки модуля
│   ├── services/          # API сервисы
│   └── index.ts           # Публичный API модуля
├── shared/                 # Глобальные компоненты и утилиты
│   ├── components/        # Переиспользуемые UI компоненты (React Native Elements)
│   ├── hooks/             # Глобальные хуки
│   ├── contexts/          # React контексты
│   ├── constants/         # Глобальные константы
│   └── theme/             # Тема React Native Elements
│       ├── themeAdapter.ts # Адаптер для интеграции с Colors.ts
│       ├── ThemeProvider.tsx # Провайдер темы
│       └── index.ts       # Экспорт темы
└── types/                  # TypeScript типы
    ├── index.ts           # Главный экспорт типов
    ├── auth.ts            # Типы аутентификации
    ├── coffee-shops.ts    # Типы кофеен
    └── orders.ts          # Типы заказов
```

### 🎯 Основные принципы

- **React Native Elements**: ВСЕГДА используйте React Native Elements для базовых UI компонентов
- **Модульность**: Каждый модуль имеет четкую ответственность
- **Переиспользование**: Компоненты и хуки должны быть переиспользуемыми
- **Типизация**: Строгая TypeScript типизация для всех сущностей
- **Цветовая система**: Использование только палитры Colors.ts
- **Архитектура**: Следование установленной структуре проекта
- **Адаптивность**: Все компоненты должны работать на мобильных устройствах и планшетах (без desktop)
- **Готовые решения**: ВСЕГДА сначала ищите готовые библиотеки для сложных задач
- **Экранные компоненты**: Размещение в папке `app/` с группировкой по модулям

### 🔧 Создание новых файлов

#### В модуле auth:

- Специфичные для аутентификации компоненты (используйте React Native Elements)
- Хуки для работы с данными аутентификации
- Валидация форм аутентификации

#### В модуле coffee-shops:

- Специфичные для кофеен компоненты (используйте React Native Elements)
- Хуки для работы с данными кофеен
- API сервисы для кофеен

#### В shared:

- Переиспользуемые UI компоненты (используйте React Native Elements)
- Глобальные хуки и контексты
- Общие константы и утилиты
- Тема React Native Elements

#### В types:

- Централизованные типы для всего проекта
- Интерфейсы для API и данных
- Utility types и generic типы

#### В app/:

- Экранные компоненты с группировкой по модулям
- Использование Expo Router для навигации

### 📝 Примеры использования

#### Создание компонента с React Native Elements:

```typescript
// Следуйте правилам из components.mdc
import React from 'react';
import { ViewStyle } from 'react-native';
import { Button, Card, Text } from '@rneui/themed';
import { useCoffeeFlowTheme } from '../../shared/theme';
import type { ComponentProps } from '../../types';

interface MyComponentProps {
  title: string;
  onPress: () => void;
}

const MyComponent: React.FC<MyComponentProps> = ({ title, onPress }) => {
  const { theme } = useCoffeeFlowTheme();

  const cardStyle: ViewStyle = {
    backgroundColor: theme.colors.background,
  };

  return (
    <Card containerStyle={cardStyle}>
      <Card.Title>{title}</Card.Title>
      <Button title="Нажми меня" onPress={onPress} color="primary" />
    </Card>
  );
};

export default MyComponent;
```

#### Создание экрана:

```typescript
// Следуйте правилам из components.mdc
import React from 'react';
import { ScrollView, StyleSheet } from 'react-native';
import { Text, Button } from '@rneui/themed';
import { useCoffeeFlowTheme } from '../../shared/theme';

const Screen: React.FC = () => {
  const { theme } = useCoffeeFlowTheme();

  return (
    <ScrollView style={styles.container}>
      <Text h1>Заголовок экрана</Text>
      <Button title="Действие" onPress={() => {}} color="primary" />
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
});

export default Screen;
```

#### Создание хука:

```typescript
// Следуйте правилам из hooks.mdc
import { useState, useCallback } from 'react';
import type { HookReturnType } from '../../types';

interface UseMyHookParams {
  initialValue?: string;
  onSuccess?: (value: string) => void;
}

interface UseMyHookReturn {
  value: string;
  setValue: (value: string) => void;
  handleAction: () => void;
}

const useMyHook = ({
  initialValue = '',
  onSuccess,
}: UseMyHookParams = {}): UseMyHookReturn => {
  const [value, setValue] = useState(initialValue);

  const handleAction = useCallback(() => {
    if (onSuccess) {
      onSuccess(value);
    }
  }, [value, onSuccess]);

  return { value, setValue, handleAction };
};

export default useMyHook;
```

### ⚠️ Важные напоминания

1. **ВСЕГДА используйте React Native Elements** для базовых UI компонентов
2. **ВСЕГДА используйте цвета из палитры Colors.ts**
3. **Следуйте модульной архитектуре проекта**
4. **Типизируйте все компоненты и хуки**
5. **Создавайте переиспользуемые сущности**
6. **Используйте barrel exports через index.ts файлы**
7. **Обеспечивайте адаптивность для всех устройств**
8. **Размещайте экранные компоненты в папке app/ с группировкой по модулям**

### 🔍 Проверка правил

Перед коммитом убедитесь, что:

- ✅ Все UI компоненты используют React Native Elements
- ✅ Все цвета взяты из палитры Colors.ts
- ✅ Компоненты следуют правилам из components.mdc
- ✅ Хуки следуют правилам из hooks.mdc
- ✅ Типы следуют правилам из typescript.mdc
- ✅ Структура соответствует архитектуре из architecture.mdc
- ✅ Компоненты адаптивны для мобильных устройств и планшетов (без desktop)
- ✅ Для сложных задач рассмотрены готовые библиотеки из libraries.mdc
- ✅ Экранные компоненты размещены в папке app/ с группировкой по модулям

### 📚 Дополнительные ресурсы

- [React Native документация](https://reactnative.dev/)
- [React Native Elements документация](https://reactnativeelements.com/)
- [TypeScript документация](https://www.typescriptlang.org/)
- [React хуки документация](https://react.dev/reference/react/hooks)
- [Expo Router документация](https://docs.expo.dev/router/introduction/)
- [Coffee Flow цветовая система](./colors.mdc)
- [Coffee Flow адаптивная верстка](./responsive.mdc)
- [Coffee Flow использование библиотек](./libraries.mdc)

**Помните**: Эти правила созданы для обеспечения качества, консистентности и поддерживаемости кода. **ОБЯЗАТЕЛЬНО используйте React Native Elements** для всех UI компонентов и размещайте экранные компоненты в папке `app/` с группировкой по модулям! 🚀
